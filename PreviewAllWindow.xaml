<Window x:Class="WPFImageTransfer.PreviewAllWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ảnh trong danh sách" Height="600" Width="800" Background="#202124">
    <Grid Margin="10">
        <ListBox x:Name="icPreviews" Background="Transparent" BorderThickness="0"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.ScrollUnit="Pixel">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#45474D" BorderThickness="1" Margin="5" Padding="5" Background="#292A2D" 
                            PreviewMouseLeftButtonDown="Border_PreviewMouseLeftButtonDown" Cursor="Hand" 
                            ToolTip="Nhấp đúp (Double-click) để chuyển sang ảnh này" HorizontalAlignment="Center">
                        <Image Source="{Binding}" Width="250" Height="180" Stretch="Uniform"/>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</Window>
